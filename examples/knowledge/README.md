# Optional knowledge

Put domain references here: design notes, papers, API docs, prior art, architecture
constraints, benchmark methodology, or known-good examples.

The autoresearch skill treats knowledge as optional reference material. It should
search/read relevant files when they reduce uncertainty rather than loading the
whole directory into context on every iteration.
